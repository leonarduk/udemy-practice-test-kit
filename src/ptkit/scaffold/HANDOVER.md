# Handover

What the next person — or the next session — needs to know to pick this up.
Keep it current; this file is the reason a fork of this repo starts in hours
rather than days.

## Current state

- Exams authored: TODO / 6
- Questions written: TODO / TODO
- Live on Udemy: no / course id TODO
- `ptkit validate` clean? TODO

## Where the content came from

TODO: transcribed from an upstream repo? authored directly? A transcription is
a reformatting-only exercise — say so explicitly, and say whether the content
was independently re-verified here, because a reader will otherwise assume it
was.

## The authoring standard

See `questions-master.md`'s header. The two that are routinely violated:

- every code-output claim verified by actually running the code;
- option-length parity treated as a constraint while writing, not a cleanup
  pass afterwards.

## Judgment calls made so far

TODO: anywhere this bank deliberately deviates from its source, its sibling
repos, or the obvious reading of the exam guide — and why.

## Known caveats

TODO: what is not verified, not finished, or known to be weak.

## Platform gotchas

Not here — they live in the kit's `UDEMY-PLATFORM-NOTES.md`, shared by every
course. Add new ones there, not to this file.
